### Light and dark modes

This template has light and dark mode support. The `useColorScheme()` hook lets you inspect what the user's current color scheme is, so that you can adjust UI colors accordingly.
