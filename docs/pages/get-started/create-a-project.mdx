---
title: Create a project
description: Learn how to create a new Expo project.
hideTOC: true
---

import { Terminal } from '~/ui/components/Snippet';
import { TemplateFeatures } from '~/scenes/get-started/create-a-project/TemplateFeatures';
import { ProjectStructure } from '~/scenes/get-started/create-a-project/ProjectStructure';

System requirements:

- [Node.js (LTS)](https://nodejs.org/en/).
- macOS, Windows (Powershell and WSL 2), and Linux are supported.

We recommend creating a project with the default project template, which includes example code to help get you started. To create a new project, run the following command:

<Terminal cmd={['$ npx create-expo-app@latest']} />

> You can use another template by adding the `--template` flag.

## Features

The default project template has the following features:

<TemplateFeatures />

## File structure

Below, you can get familiar with the default project's file structure:

<ProjectStructure />

### Next steps

You have a project. Now it's time to set up your development environment so that you can start developing.
